/**
    const logSheet = wflFile.getSheetByName('Log');
    logSheet.clear();
    const logs = [
      ["e['value']",e['value']],
      ["eRow",eRow],
      ["eColumn",eColumn],
      ["statusMBO",statusMBO],
      ["ckBox",ckBox],
      ["endCol_MBO",endCol_MBO],
      ["runFlag",runFlag]
    ];
    logSheet.getRange(1,1,logs.length,logs[0].length).setValues(logs);
*/